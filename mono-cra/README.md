# Create React App x TypeScript

This is a Create React App x TypeScript application (via `react-scripts-ts`).

If you look at `package.json`, you'll notice that it has `@mono/common` and `@mono/ui` as dependencies.
